# 使用单体轻量服务保存共享数据

系统将作为一个 Node.js 轻量服务部署在 VPS 上，同一服务既提供前端页面，也提供少量服务端接口；共享数据保存在 SQLite 单文件数据库中。系统不依赖 MySQL 等独立数据库服务，也不采用 Spring Boot 等重型后端框架。浏览器只负责界面与交互，因为多人需要从不同设备共享数据，浏览器本地存储不能作为权威数据源，也无法提供可信的身份认证与权限隔离。
