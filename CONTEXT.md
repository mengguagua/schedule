# 护士排班

本上下文描述医院护士账户、护士组与月度排班所使用的统一业务语言。

## Language

**超级用户**：
系统预置且登录账号固定为 `admin` 的管理账户，不可改名、删除或停用，负责创建和管理普通账户，但不属于护士组，也不参与排班；其密码可以修改，首次登录不强制修改。
_Avoid_：管理员护士、护士长账户

**普通账户**：
由超级用户创建的登录账户；账户可被分入护士组并成为排班对象，登录后可只读查看自己所在护士组所有已生成月份的完整排班。离职账户只停用而不永久删除：停用后不能登录或进入后续排班，并自动移出当前组，但不改变已有排班和操作日志；账户可以重新启用。
_Avoid_：员工档案、护士档案

**登录账号**：
普通账户用于登录的全系统唯一标识，创建后不可修改。
_Avoid_：护士姓名、工号

**护士姓名**：
普通账户在排班表中显示的名称，可以修改且允许重名；同组重名时以“姓名（登录账号）”区分。改名后，过去月份保留当时的姓名快照，当前月和未来月显示新姓名而不改变班次。
_Avoid_：登录账号、唯一标识

**护士组**：
月度排班的主体，由若干普通账户组成；一个普通账户同一时间最多属于一个护士组，也可以暂时未分组。月度排班保存生成时的成员快照，之后的入组、出组或转组不会自动改变已有结果。从未生成排班的空组可以删除；已有排班或日志的组只能归档。归档组停止自动排班和成员加入，但保留历史并可恢复。
_Avoid_：班次、科室

**排班规则**：
护士组统一配置的月休天数与每日休息人数。修改规则不会改变已有排班单元格；当前月和未来月按新规则重算提示，过去月份使用当时保存的规则快照。
_Avoid_：排班结果、个人规则

**月休天数**：
护士组统一配置的规则，表示组中每名护士在一个完整自然月内必须获得的休息天数；一键排班优先满足该规则，不支持为单个护士另设额度。非完整月或中途入组、离组时，个人额度按其排班区间内工作日占全月工作日的比例四舍五入。若额度超过目标排班区间内的工作日数量，一键排班必须停止并提示修正规则，不能自动使用周末补足。
_Avoid_：每日休息人数、假期余额

**每日休息人数**：
护士组在周一至周五每天期望安排的休息人数；取值必须是从零到当前组员人数的整数。周六和周日的默认目标为零，系统不自动识别法定节假日或调休。每日目标与月休天数无法同时满足时，允许以最小偏差突破并产生规则提示；排班管理员也可以手工调整周末结果。
_Avoid_：每日休息上限、硬性人数

**长白班**：
护士在排班区间内某个日期正常工作的排班状态，也是所有护士每天的默认状态。
_Avoid_：白班、上班

**休息**：
护士在排班区间内某个日期不安排长白班的排班状态；它是第一版唯一的非工作状态。
_Avoid_：请假、夜班、调休

**排班区间**：
某名护士实际参与一份月度排班的日期范围；中途入组者从入组日开始，中途离组者在离组日前结束。区间外的单元格留空，不属于班次状态。
_Avoid_：空班、第三班次

**规则提示**：
排班结果存在工作日休息人数偏差、个人月休天数偏差、周末休息或连续工作日休息时，在排班表下方列出具体日期与护士姓名的文字说明；它不阻止一键排班或后续手工修改。
_Avoid_：保存错误、阻断校验

**手工调整**：
排班管理员在月度排班中批量切换“长白班”和“休息”的操作；未保存改动高亮并实时重算规则提示，规则不符时经确认仍可整批保存，全部变更归入同一条操作日志。如果班表已被他人更新，本次保存必须停止并要求刷新，不能覆盖或自动合并。
_Avoid_：一键排班、即时保存

**均匀休息**：
一键排班时尽量将同一护士的休息日分散，并避免连续两个工作日休息；生成新月份时，如果上月已有排班，还需参考上月最后一个工作日以避免跨月连休。这是优化目标而非硬性限制，无法满足时仍可生成并产生规则提示。
_Avoid_：强制间隔、连续休息禁令

**一键排班**：
在目标月份尚无月度排班时，依据护士组成员、组级规则和必要的上月排班生成整月班表的操作；手工执行仅支持当前月和下一个月。算法必须稳定可复现，相同输入始终产生相同结果。目标月份已有排班时不能再次执行。
_Avoid_：随机排班、自动发布

**自动月排班**：
系统按 `Asia/Shanghai` 时区在每月 1 日 00:05，自动为当月尚无排班的护士组执行一键排班，是正常情况下创建月度排班的主要方式。服务错过执行时间后，应在恢复启动时补生成缺少的当月排班；操作日志中的操作者记为“系统”。遇到空组或无效配置时只跳过该组，不影响其他组；异常通知仅向超级用户显示，本组排班页仍需说明未生成原因。
_Avoid_：手工重排、定期覆盖

**月度排班**：
某个护士组在目标月份内唯一的一份排班结果；当前月从执行一键排班的当天开始至月末，未来月份覆盖完整自然月。过去月份只读，当前月和未来月份允许手工修改，也可在二次确认后整份删除。删除后才允许重新执行一键排班。当前月中途入组的护士可由排班管理员从入组当天添加到已有排班；中途离组的护士保留此前记录，并从离组日起结束排班区间。
_Avoid_：排班草稿、排班版本

**排班管理权限**：
超级用户可为普通账户开启或关闭的操作权限，允许该账户管理自己所在护士组的成员、规则与班表，但不能创建、改名、归档或恢复护士组；成员管理仅限添加未分组且没有排班管理权限的账户或移出本组普通护士，不能移出自己或其他排班管理员，跨组转移与排班管理员的成员变更只能由超级用户执行。超级用户可管理所有护士组。该权限与账户是否使用默认密码无关，重置密码也不改变权限。护士组可以没有获得此权限的普通账户，此时仅由超级用户管理。该权限不影响账户作为护士参与排班，也不改变已有排班结果。
_Avoid_：可排班资格、护士身份、员工类型

**操作日志**：
对系统内成功新增、修改和删除操作所永久保留的审计记录，用于追溯操作者、时间、护士组、操作对象及变更前后内容；自动任务的操作者记为“系统”。任何账户（包括超级用户）都不能修改或删除该记录。超级用户可查看全系统日志，拥有排班管理权限的普通账户只能查看自己所在护士组的日志，其他账户不可查看；查看行为和 IP 地址不记录。
_Avoid_：排班备注、通知记录
